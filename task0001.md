- 为Xxlang增加getConfigObj函数，获取Xxlang的配置对象，这是从一个JSON格式的文件读取的。查找这个文件的路径优先级如下：先在用户目录的`.xxl`子目录下搜寻settings.json文件，如果没有，则如果是Linux类系统，到`/.xxl`目录下寻找，如果是Windows，则在`C:\.xxl`目录下寻找；
- Xxlang的主程序应该通过命令行开关参数"-cloud"，表示从用户设置的远程路径（cloudUrlBase）为基路径执行脚本，例如：如果用户在Xxlang配置对象里设置了字段cloudUrlBase：
```json
{
	"cloudUrlBase": "https://script.topget.org/"
}
```

则如果命令行执行：
`xxl -cloud basic.xxl`
表示从 `https://script.topget.org/basic.xxl` 处获取脚本内容并执行

- 为Xxlang增加getConfigStr函数，用于获取Xxlang的配置字段，目录查找方法与getConfigObj相同，最后同同名加.cfg后缀的文本文件中读取全部内容，例如：
`getConfigStr("abc")` 将从按顺序查找那些目录下的`abc.cfg`文件，如果有，则读取其中所有内容返回字符串；
- 增加相对于getConfigStr函数的setConfigStr函数，用于设置用户自定义的配置字符串，例如： `setConfigStr("abc", "dictionary")` ，将在第一个符合查找顺序规则的目录下建一个abc.cfg文件（如果已有则覆盖），然后将其内容置为 `dictionary` 
