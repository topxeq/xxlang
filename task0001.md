- 为Xxlang增加getConfigObject函数，获取Xxlang的配置对象，这是从一个JSON格式的文件读取的。查找这个文件的路径优先级如下：先在用户目录的`.xxl`子目录下搜寻settings.json文件，如果没有，则如果是Linux类系统，到`/.xxl`目录下寻找，如果是Windows，则在`C:\.xxl`目录下寻找；
- Xxlang的主程序应该通过命令行开关参数"-cloud"，表示从用户设置的远程路径（cloudUrlBase）为基路径执行脚本，例如：如果用户在Xxlang配置对象里设置了字段cloudUrlBase：
```json
{
	"cloudUrlBase": "https://script.topget.org/"
}
```

则如果命令行执行：
`xxl -cloud basic.xxl`
表示从 `https://script.topget.org/basic.xxl` 处获取脚本内容并执行

