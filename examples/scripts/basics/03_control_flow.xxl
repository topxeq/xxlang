// 03_control_flow.xxl
// If-else statements and conditional expressions

// === Basic If Statement ===
var age = 18

pln("=== Basic If Statement ===")
if (age >= 18) {
    pln("You are an adult")
}

// === If-Else Statement ===
var score = 75

pln("\n=== If-Else Statement ===")
if (score >= 60) {
    pln("Passed!")
} else {
    pln("Failed")
}

// === Nested If-Else Chain ===
// Note: Xxlang uses nested if-else for multi-way conditions
pln("\n=== Nested If-Else Chain ===")
var grade = 85

if (grade >= 90) {
    pln("Grade: A")
} else {
    if (grade >= 80) {
        pln("Grade: B")
    } else {
        if (grade >= 70) {
            pln("Grade: C")
        } else {
            if (grade >= 60) {
                pln("Grade: D")
            } else {
                pln("Grade: F")
            }
        }
    }
}

// === Nested If Statements ===
pln("\n=== Nested If Statements ===")
var isLoggedIn = true
var isAdmin = true

if (isLoggedIn) {
    if (isAdmin) {
        pln("Welcome, Admin!")
    } else {
        pln("Welcome, User!")
    }
} else {
    pln("Please log in")
}

// === Switch-like Pattern (using if-else) ===
pln("\n=== Switch-like Pattern ===")
var day = 3

// Xxlang uses if-else chains for switch-like behavior
if (day == 1) {
    pln("Monday")
} else {
    if (day == 2) {
        pln("Tuesday")
    } else {
        if (day == 3) {
            pln("Wednesday")
        } else {
            if (day == 4) {
                pln("Thursday")
            } else {
                if (day == 5) {
                    pln("Friday")
                } else {
                    if (day == 6) {
                        pln("Saturday")
                    } else {
                        if (day == 7) {
                            pln("Sunday")
                        } else {
                            pln("Invalid day")
                        }
                    }
                }
            }
        }
    }
}

// === Season Example ===
pln("\n=== Season Example ===")
var month = 2
var season = ""

if (month == 12 || month == 1 || month == 2) {
    season = "Winter"
} else {
    if (month >= 3 && month <= 5) {
        season = "Spring"
    } else {
        if (month >= 6 && month <= 8) {
            season = "Summer"
        } else {
            if (month >= 9 && month <= 11) {
                season = "Autumn"
            } else {
                season = "Unknown"
            }
        }
    }
}

pln("Month ", month, " is ", season)

// === Conditional Logic ===
pln("\n=== Conditional Logic ===")
var num = 15
var parity = ""

if (num % 2 == 0) {
    parity = "even"
} else {
    parity = "odd"
}

pln(num, " is ", parity)

// === Complex Conditions ===
pln("\n=== Complex Conditions ===")
var hasLicense = true
var hasInsurance = true
var age2 = 25

if (age2 >= 18 && hasLicense && hasInsurance) {
    pln("You can drive legally")
} else {
    pln("You cannot drive legally")
}

// === Short-Circuit Example ===
pln("\n=== Short-Circuit Evaluation ===")
var divisor = 0
var dividend = 10

// Check divisor first to prevent division by zero
if (divisor == 0) {
    pln("Cannot divide: divisor is zero")
} else {
    if (dividend / divisor > 5) {
        pln("Result is greater than 5")
    } else {
        pln("Result is too small")
    }
}
