// Test nested call with zero-arg function
import { getCounter, increment, add } from "./utils"

// Test getCounter (reads global, 0 args)
pln("Counter: ", getCounter())

// Test increment (writes global, 0 args)
pln("After increment: ", increment())
